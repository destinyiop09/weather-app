[project]
name = "weather-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Emmanuel" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "requests>=2.34.2",
    "uvicorn>=0.52.4",
]

[project.scripts]
weather-app = "weather_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
